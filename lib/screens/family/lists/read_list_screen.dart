import 'package:flutter/material.dart';

class ReadListScreen extends StatelessWidget {
  const ReadListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('read list screen'),
    );
  }
}
